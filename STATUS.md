# Текущий статус разработки (KManim)

**Активный спринт:** Спринт 0 (Каркас)

**Задача в работе:** Инициализация проекта, настройка репозитория, создание Gradle-каркаса с `build-logic` и централизованным каталогом версий.

**Что уже готово:**
- Техническое задание обновлено до версии **v0.14** (`KManim_TZ-014.md`), внедрены хард-блокировки генерации HTML-зеркала документации ради исключения упущений со стороны ИИ-агентов.
- Созданы и ведутся файлы отслеживания прогресса (`STATUS.md`, `DEVLOG.md`, `API_PARITY.md`).
- Утверждены стандарты: композитная сборка `includeBuild("build-logic")`, скриншот-тесты `Roborazzi`, команда верификации `./gradlew check assembleDebug generateMirrorDocs`.

**Следующий шаг:**
- Инициализировать корневой каталог `gradle/libs.versions.toml` (AGP 8.4+, Kotlin 2.0+, JUnit5, Roborazzi).
- Создать каркас `build-logic` и основные Gradle-модули (`manim-core`, `manim-geometry`, `manim-render`, `demo-app`).

**Как продолжить:**
Прочитайте ТЗ v0.14 (`md/KManim_TZ-014.md`), если вы в проекте впервые. Приступайте к созданию структуры файлов Android-проекта, `gradle/libs.versions.toml` и `build-logic`.

**Последний значимый коммит:** `e80a3a8`
