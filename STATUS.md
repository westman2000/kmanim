# Текущий статус разработки (KManim)

**Активный спринт:** Спринт 0 (Каркас)

**Задача в работе:** Ревизия ТЗ до v0.17 завершена; следующий блок — создание Gradle-каркаса с `build-logic` и централизованным каталогом версий.

**Что уже готово:**
- Техническое задание обновлено до версии **v0.17** (`md/KManim_TZ-017.md`): по итогам глубокого многоракурсного аудита внесены решения **D-34…D-48** (терминология геометрии, контракт `Animation`, мат-типы, кадровый такт, 2D-камера, `VGroup`, жизненный цикл ресурсов, ассеты/ошибки формул, `Tex`/индексация/`matchPoints`, имена rate-функций, модель ввода, спецификация `generateMirrorDocs`, зафиксированный стек версий и build-параметры, двухуровневый Definition of Done, якорь состояния/push); исправлены факты паритета ManimCE (галерея, туториалы, дефолты rate-функций) и устранены внутренние противоречия (в т. ч. некомпилируемый эталонный код).
- Добавлены разделы 6.1 (параметры сборки модулей), 7.1 (зафиксированный стек версий), 5.5.6 (2D-камера), 5.5.7 (жизненный цикл ресурсов), 5.10 (модель ввода).
- Трекеры (`STATUS.md`, `DEVLOG.md`, `API_PARITY.md`) синхронизированы с реальной git-историей (устранены фантомные хеши коммитов).
- Утверждены стандарты: композитная сборка `includeBuild("build-logic")` с тремя convention-плагинами (`kmanim.jvm.library` / `kmanim.android.library` / `kmanim.android.application`), единый golden-инструмент `Roborazzi`, команда верификации `./gradlew check assembleDebug generateMirrorDocs`.

**Definition of Done — два уровня (раздел 14.2, D-47):**
- *На каждый коммит:* линтинг + JVM unit + быстрый Roborazzi ≥98% + APK + `generateMirrorDocs`.
- *На закрытие спринта (перед тегом `sprint-N-done`):* + эталонный on-device golden ≥98% (`connectedCheck`) + golden «manim-parity» + `API_PARITY.md` + `STATUS.md`/`DEVLOG.md`; коммит и тег **запушены на origin**.

**Следующий шаг:**
- Закоммитить и запушить ревизию ТЗ v0.17 (переименование `md/KManim_TZ-016.md` → `md/KManim_TZ-017.md` + правки трекеров).
- Инициализировать `gradle/libs.versions.toml` по разделу 7.1 (точные координаты/версии) и каркас `build-logic` с тремя convention-плагинами (раздел 6.1).
- Создать основные Gradle-модули (`manim-core`, `manim-geometry` — Kotlin/JVM; `manim-render`, `demo-app` — Android) с namespace/SDK/JDK из раздела 6.1.
- Завести Gradle-задачу `generateMirrorDocs` (каркас `docs/` со 100% описью ManimCE в статусе ⬜; раздел 10.1 / D-45).

**Как продолжить (входной ритуал, 14.4):**
Прочитайте `STATUS.md` → хвост `DEVLOG.md` (включая «Грабли») → `API_PARITY.md`, затем ТЗ v0.17 (`md/KManim_TZ-017.md`), если вы в проекте впервые. Сверьте «последний значимый коммит» с `git HEAD`. Затем приступайте к каркасу Спринта 0 (разделы 6.1, 7.1, 9 — Спринт 0).

**Последний значимый коммит:** `ee54088` (v0.17 — ревизия по аудиту), запушен на `origin/main`.
